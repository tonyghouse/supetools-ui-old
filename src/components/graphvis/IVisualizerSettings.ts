export interface IVisualizerSettings {
  // Direction property takes one of the defined CanvasDirection values.
  // A default value can be set when creating instances if needed.
  direction: CanvasDirection;
}